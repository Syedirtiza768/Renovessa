# Component Map

> **Status:** Planned — no components exist yet.

## Layout Components (Planned)

| Component | Purpose | Location (planned) |
|-----------|---------|-------------------|
| `AppShell` | Header + main layout | `components/layout/AppShell` |
| `Header` | Logo, nav, user menu | `components/layout/Header` |
| `PageContainer` | Max-width wrapper | `components/layout/PageContainer` |

## Auth Components (Planned)

| Component | Purpose |
|-----------|---------|
| `LoginForm` | Email/password login |
| `RegisterForm` | New user signup |

## Project Components (Planned)

| Component | Purpose |
|-----------|---------|
| `ProjectCard` | Summary on dashboard |
| `ProjectForm` | Create/edit project |
| `ProjectHeader` | Title, status badge, actions |
| `TaskList` | List of tasks |
| `TaskItem` | Single task row with checkbox |
| `TaskForm` | Add task inline or modal |

## Shared UI (Planned)

| Component | Purpose |
|-----------|---------|
| `Button` | Primary actions |
| `Input` | Text fields |
| `EmptyState` | No data messaging |
| `Spinner` | Loading indicator |
| `ConfirmDialog` | Delete confirmations |

## Forms

- Client-side validation for required fields
- Server errors mapped to fields

## Tables

Not required for MVP — card/list layout preferred for projects.

## Modals

- Delete project confirmation
- Invite member (later)

## Implementation Status

All components: **Planned**
